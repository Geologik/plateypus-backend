dist: trusty
sudo: required
language: python
python: 
    - '3.6'
install:
    - 'pip install pipenv'
    - 'pipenv sync' 
addons:
    sonarcloud:
        organization: 'kristian-thy50409'
script:
    - 'python -m pytest --cov-report term-missing:skip-covered --cov=plateypus'
    - 'sonar-scanner'
